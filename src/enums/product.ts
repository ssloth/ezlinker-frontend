export enum CMD_VALUE {
  number = 1,
  string = 2,
  boolean = 3,
  JSON = 4,
}

export enum FEATURE_TYPE {
  button,
  switch,
}

export enum PROTOCOL_TYPE {
  MQTT,
  COAP,
  HTTP,
  TCP,
}

export enum PRODUCT_TYPE {
  通用,
}
