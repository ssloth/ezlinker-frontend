import React from 'react';
import styles from './VirtualDevice.less';

const VirtualDevice = () => (
  <div className={styles['virtual-device-manage']}>
    <h3 className={styles.title}>虚拟设备管理</h3>
  </div>
);

export default VirtualDevice;
